import styled from 'styled-components';

const Select = styled.select`
  line-height: 1em;
  height: 30px;
  color: #fff5f5;
  // color: black;
  &:active{
    background: #312d3b;
  }
`;

export default Select;
